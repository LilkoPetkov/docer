# TODOs

1. Report directory should be `dynamic` passed via args
2. Sort by functions with definitions and functions without them -> allow filtering of only those who have no fds
3. Export report into files: `YAML` 
4. Concurrency for the processing
